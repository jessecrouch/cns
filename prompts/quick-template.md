Generate a CNS program that accomplishes the following task:

{TASK}

Requirements:
- Use proper CNS syntax (Story, Given, Step with Because, End)
- Include type annotations for all variables
- Every step must have a Because clause explaining why
- Use appropriate data types (Integer, String, List, Map)
- Follow CNS expression rules (variable-first, not literal-first)
- Make the code clear and self-documenting

Generate ONLY the CNS code without any markdown formatting or explanations.
