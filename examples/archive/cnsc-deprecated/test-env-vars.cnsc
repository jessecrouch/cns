Story: Test Environment Variables
G: api_key:S, user:S, port:S, missing:S

S1→ Get API key
  api_key=ENV("TEST_API_KEY", "default_key_123")

S2→ Get username
  user=ENV("USER", "unknown")

S3→ Get port with default
  port=ENV("APP_PORT", "8080")

S4→ Get missing var
  missing=ENV("NONEXISTENT_VAR", "fallback")

S5→ Display all
  E: PRINT "API: {api_key}"
  E: PRINT "User: {user}"
  E: PRINT "Port: {port}"
  E: PRINT "Missing: {missing}"

End: ENV vars working
