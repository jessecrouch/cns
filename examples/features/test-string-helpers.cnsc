G: text:S = "  Hello World  "
G: list:L = ["apple", "banana", "cherry"]
G: messy:S = "the quick brown fox"
G: cleaned:S, upper:S, lower:S, replaced:S, joined:S
G: length1:I, length2:I
G: combined:S

S1→ Test operations
  cleaned = TRIM text
  upper = UPPERCASE cleaned
  lower = LOWERCASE cleaned
  replaced = REPLACE "World" WITH "CNS" IN cleaned
  joined = JOIN list WITH ", "
  length1 = LENGTH_OF cleaned
  length2 = LENGTH_OF list

S3→ Test combined operations
  combined = TRIM "  spaces  "
  combined = UPPERCASE combined

S2→ Display results
  E: PRINT "Original: '" + text + "'"
  E: PRINT "Trimmed: '" + cleaned + "'"
  E: PRINT "Uppercase: " + upper
  E: PRINT "Lowercase: " + lower
  E: PRINT "Replaced: " + replaced
  E: PRINT "Joined: " + joined
  E: PRINT "String length: " + STR length1
  E: PRINT "List length: " + STR length2
  E: PRINT "Combined ops: " + combined

End: String helpers working
