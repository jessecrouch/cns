Story: Simple Database Test

G: db_file:S="test.db", result:S=""

S1→ Effect: DB CONNECT TO "test.db" AS mydb
S2→ Effect: DB EXECUTE "CREATE TABLE IF NOT EXISTS users (id INTEGER PRIMARY KEY, name TEXT)" AS mydb
S3→ Effect: DB EXECUTE "INSERT INTO users (name) VALUES ('Alice')" AS mydb
S4→ Effect: DB EXECUTE "INSERT INTO users (name) VALUES ('Bob')" AS mydb
S5→ Effect: DB QUERY "SELECT * FROM users" AS mydb INTO result
S6→ Effect: Print "Query result: {result}"

E: result
