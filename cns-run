#!/bin/bash
# CNS Runner Wrapper - calls the actual runner in src/
# This allows running from project root: ./cns-run file.cns

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/src/cns-run" "$@"
